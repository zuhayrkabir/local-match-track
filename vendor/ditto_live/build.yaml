targets:
  $default:
    builders:
      json_serializable:
        options:
          # We want `.toJson()` serialization to apply recursively on objects that support it.
          explicit_to_json: true
          field_rename: snake
